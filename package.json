{
  "name": "autoprompter",
  "version": "2.0.0",
  "private": true,
  "description": "Microsoft Edge extension that cycles through selected ChatGPT conversations and sends configurable continuation prompts.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check background.js && node --check content.js && node --check popup.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "Apache-2.0"
}
